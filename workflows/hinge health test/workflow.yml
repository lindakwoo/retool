pythonLanguageConfiguration:
  object:
    id: 1448e87a-df12-4e56-82b4-4d2293c0a1c4
    name: workflow_python_6301cf3c-3153-4ea2-a3e3-888064aefa33
    description: null
    libraries:
      object:
        bson:
          object:
            specVersion: 0.5.10
            actualVersion: ''
        numpy:
          object:
            specVersion: 1.24.1
            actualVersion: ''
        pandas:
          object:
            specVersion: 1.5.2
            actualVersion: ''
    librariesFormat: requirements.txt
    language: python
    pythonVersion: null
private: false
customLibraries:
  array:
    - object:
        version: 0.5.10
        language: python
        codeString: ''
        libraryName: bson
    - object:
        version: 1.24.1
        language: python
        codeString: ''
        libraryName: numpy
    - object:
        version: 1.5.2
        language: python
        codeString: ''
        libraryName: pandas
    - object:
        version: 4.18.1
        language: javascript
        variable: _
        codeString: |
          /* Edit library variable below */

          const _ = require('lodash')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: lodash
    - object:
        version: 2.1.0
        language: javascript
        variable: numbro
        codeString: |
          /* Edit library variable below */

          const numbro = require('numbro')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: numbro
    - object:
        version: 5.3.2
        language: javascript
        variable: Papa
        codeString: |
          /* Edit library variable below */

          const Papa = require('papaparse')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: papaparse
    - object:
        version: 0.5.23
        language: javascript
        variable: moment
        codeString: |
          /* Edit library variable below */

          const moment = require('moment-timezone')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: moment-timezone
    - object:
        version: 3.4.0
        language: javascript
        variable: uuid
        codeString: |
          /* Edit library variable below */

          const uuid = require('uuid')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
        libraryName: uuid
name: hinge health test
setupScripts:
  object:
    python:
      object:
        codeString: ''
    javascript:
      object:
        codeString: |
          // lodash
          /* Edit library variable below */

          const _ = require('lodash')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // numbro
          /* Edit library variable below */

          const numbro = require('numbro')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // papaparse
          /* Edit library variable below */

          const Papa = require('papaparse')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // moment-timezone
          /* Edit library variable below */

          const moment = require('moment-timezone')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */

          // uuid
          /* Edit library variable below */

          const uuid = require('uuid')

          /* Add destructured imports from library below
          eg. const { pow, log } = require("mathjs") */
hasProtectedTriggers: true
triggerWebhooks:
  array:
    - object:
        name: startTrigger
        uuid: startTrigger
        headers: '{}'
        inputSchema:
          object:
            properties:
              array: []
        useHeaderApiKey: false
        exampleInputJSON: ''
        examplePathParams: ''
version: 43.0.9
workflowId: 7b212cb8-1fb9-4288-ac5e-3d3b454bbee1
description: null
workflowTemplate:
  pubAppDecoupledQueriesDisabled: null
  version: 4.37.0
